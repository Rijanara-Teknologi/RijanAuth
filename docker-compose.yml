version: '3.8'
services:
  appseed-app:
    container_name: appseed_app
    restart: always
    build: .
    ports:
      - "5085:5085"
    networks:
      - db_network
networks:
  db_network:
    driver: bridge
